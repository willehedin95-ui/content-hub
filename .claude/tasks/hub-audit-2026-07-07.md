# Hub-audit del 2 - 2026-07-07 (resten av hubben)

> Fortsättning på creative-pipeline-auditen 2026-07-06 (den täckte koncept/bilder/translations-på-koncept/Launch Pad/Meta-PUSH - allt åtgärdat). Denna audit täcker RESTEN: landing pages/publicering, SEO-bloggen, quiz-byggare+runtime, Meta-FÖRVALTNINGEN (post-push), tvärgående infra, verktygen. 6 parallella granskningsagenter + prod-verifiering (read-only). ~130 fynd, dedupliserade nedan. [LIVE] = verifierat mot prod-data, inte bara kod.
>
> **STATUS: ÅTGÄRD PÅGÅR (Williams "kör" 2026-07-07).** Våg 1 = 5 parallella fix-agenter (Meta-avväpning, quiz, blogg, LP, verktyg) + orkestratorns säkerhets-svep. Våg 2 = infra/observability (cron-wrappers, reconcile-utbyggnad, telegram, vercel.json). Beslut exekverade: sync+zero-spend-alert återaktiveras för Renew/doginwork; bloggautopiloten förblir AV; ab_tests+stubbar rensade.
>
> **KLART (orkestratorn, före våg 1-merge):** B1 (cron-auth på process-swipe-queue), B2 (x-import-token på bulk-import + 200-cap; RESEARCH_IMPORT_TOKEN satt i Vercel prod+preview och .env.local - WILLIAMS EXTENSION MÅSTE SKICKA HEADERN x-import-token), B4+P3-middleware (boundary-match; pixel/stats + cron-status stängda), F2 (demographics ur static-ad-prompt), F4 (segment-description städad). DB: 2 stubbar raderade, ab_tests x2 arkiverade, fast cron_runs-rad stängd, slug-dubbletter omdöpta (kanoniska = nyaste), unika index pages(workspace_id,slug) + meta_campaign_mappings(workspace_id,product,country,format) skapade (gamla mappings-indexet droppas EFTER deploy), kolumner meta_ad_performance.ad_account_id + translations.published_at tillagda, flytande-kollagen-vs-pulver backfillad till get-renew-URL.
> **B3 KVAR (Williams action):** FILLOUT_WEBHOOK_SECRET - lägg custom header i Fillout-webhookens inställningar FÖRST, sen env i Vercel (annars droppas ånger-forms = compliance-risk).
> **EFTER-DEPLOY-STÄDNING (kvar):** ta bort sömn-trion (somnbesvar, tips-for-att-somna, 1177-somnproblem) från get-renew via Shopify API; ta bort flytande-kollagen-halsobladet-kopian vid nästa halsobladet-deploy; droppa gamla mappings-indexet; 7-tecken + test-kollagen lämnade på halsobladet (bulk-republish är egen framtida uppgift).

## Verifierat FRISKT (ingen åtgärd)

- **Reconcile-cronen** (igår): kör var 30 min, allt läkt, prod ren - inga strandade translations/jobb kvar.
- **Quiz-CAPI-pengavägen i praktiken**: 0 dubbletter på event_id, 0 failed, 22 köp korrekt spårade, livegrafen utan döda steg, prod kör senaste runtime-bundle (hash-match), alla quiz-bilder på Supabase Storage.
- **Action_type-summering** (hård memory-regel): `.find()` på EN purchase-typ, ingen överlappande summering någonstans.
- **Secrets**: inga hårdkodade tokens i src/scripts, service-role-nyckeln enbart server-side, endast ofarliga NEXT_PUBLIC-vars.
- **/bcheck fail-closed**: utan BRAND_CHECK_TOKEN → 401 överallt. Stängd, inte läckande (men även oanvändbar - sätt env-varen om verktyget ska funka).
- **/review action-vägen**: workspace_id tas alltid från objektets DB-rad, cross-tabell-id failar säkert, judge:REJECT/arkiv-gates fungerar.
- **PRODUCT_URLS** i blog-writer matchar `market_product_urls` [LIVE]. Affiliate-injektionen genuint tyst utan env [LIVE]. hreflang emitterar bara verifierade alternates. PubMed-klienten solid.
- **Genesis-huvudflödet läcker INTE demographics** (selectar bara name+description).

## Rotmönster över hela hubben (samma 5 som igår + 2 nya)

1. **Tysta fel**: ignorerad `{error}` från supabase-js är standardmönstret; tre schema-mismatchar (kolumner som inte finns, onConflict mot fel constraint) har gjort hela funktioner döda i månader utan att någon märkt det.
2. **Fastnade states utan självläkning**: reconcile-cronen täcker BARA koncept-pipelinens tabeller. translations.image_status, pages.importing, swipe_jobs, video_jobs (12 st fast sedan april [LIVE]) och blog content_plan ligger utanför.
3. **Sen/ingen validering**: quiz-publish skeppar trasiga grafer; slugs valideras aldrig; pengaknappar frågar aldrig hur gammal datan är.
4. **Write-only-data**: kostnader loggas som $0 eller inte alls; ab_tests har aktiv rad ingen kod kan avsluta; cron-status övervakar crons som inte finns.
5. **Placeholder-som-innehåll**: soft-404-skal som 200, UI-löften ("continues in background") som beskriver beteende som inte finns.
6. **NYTT - Multi-workspace påklistrat i efterhand**: queries per språk/status men INTE per workspace är grundorsaken till sitemap-förorening, cross-brand-länkar, slug-stölder och seed-läckage. Systemet byggdes för 1 workspace.
7. **NYTT - "Ads off"-städningen 27 april lämnade ett halvt avväpnat system**: guards borta men pengaknapparna kvar, armerade mot 2,5 månader gammal data.

---

# P0 - Pengar/prod-risk, fixa först

## A. Meta-förvaltningen: guards av medan två konton spenderar [LIVE]

Commit `7e5dbedf` (27 apr, "no ads running") tog bort ALLA ad-crons ur vercel.json - aldrig återinförda. Men Renew (hydro13) + doginwork spenderar IDAG (senaste Purchase via doginwork-pixeln 2026-07-07 06:59). `meta_ad_performance` frusen 2026-04-21, `campaign_budget_snapshots` 2026-04-16.

| # | Fynd | Fil:rad | Fix |
|---|---|---|---|
| A1 | Bleeder-guards/kill/sync/zero-spend HELT av medan konton spenderar - bleeders kan blöda obegränsat osett | vercel.json (ad-crons saknas) | BESLUT WILLIAM: återaktivera ad-performance-sync + zero-spend-alert för Renew/doginwork, eller dokumentera 100% manuell förvaltning |
| A2 | Morning-brief-UI + Ad Tracker exekverar Meta-writes (scale +20%/pause) på apriltdata - ingen staleness-spärr | morning-brief/actions/route.ts:18-521; MetaAdsDashboardClient.tsx:242-249 | Vägra exekvera när data_date < idag-2; staleness-banner |
| A3 | ALLA pengaskrivningar i actions-routen kör UTAN runWithMetaConfig → happysleep-creds mot Renew/doginwork-objekt (bryter hård CLAUDE.md-regel) | morning-brief/actions/route.ts:47,90,176-205,248-292,340,373,424-483 | Slå upp workspace via meta_campaign_mappings → meta_config, wrappa varje action |
| A4 | Telegram-legacy-knappar armerade i gamla chattmeddelanden: graduateWinners = +20% budget/klick UTAN cooldown/tak (kompounderar); applyBudgetShifts flyttar budget enligt cross-konto-blandade shares; ingen runWithMetaConfig | telegram/webhook/route.ts:366-380,447-540,601-680 | Stubba/avväpna budget_apply_all/graduate_all/strategy_kill_all eller ge dem cooldown+cap+scoping |
| A5 | Guard-bypass: zombie-klassning (spend<50) utvärderas FÖRE testing-skyddet (dag ≤4) → nypushat adset med långsam ramp auto-killas, 4-dagars-cooldown kringgås (latent) | strategy-engine.ts:399-402; autopilot-execute/route.ts:201-206 | Flytta testing-checken före zombie-checken (skärper guarden) |

## B. Oautentiserade endpoints som spenderar/muterar

| # | Fynd | Fil:rad | Fix |
|---|---|---|---|
| B1 | `/api/cron/process-swipe-queue` HELT utan auth (enda cron-routen utan CRON_SECRET), publikt vitlistad, genererar Kie-bilder = betald spend utan inloggning + DB-mutationer | api/cron/process-swipe-queue/route.ts:31; middleware:39 | CRON_SECRET-Bearer-check som övriga |
| B2 | `/api/research/sources/bulk-import` (+ /upload) publik, "auth" = att workspace_id existerar (UUID:n delvis kända via quiz-runtime/CORS), kör Haiku per post i obegränsad posts[] | research/sources/bulk-import/route.ts:63-85; middleware:41 | Riktig auth + tak på posts.length |
| B3 | Fillout-webhooken oautentiserad: signaturkoll opt-in på FILLOUT_WEBHOOK_SECRET som inte är satt → vem som helst skapar Freshdesk-tickets (Renew+SB) + triggar ack-mejl till godtyckliga adresser | fillout-to-freshdesk/route.ts:375-382 | Sätt secret i Vercel+Fillout; gör verifiering fail-closed |
| B4 | `/api/pixel/stats` publik [LIVE 200]: läcker revenue/CAPI-count; `/api/cron-status` publik pga prefix-match `/api/cron` utan slash [LIVE 200] | middleware:37,39 | Exakt-match/trailing slash i vitlistan; session-check på stats |

## C. Quiz: en klickkedja från att ta ner livefunneln

| # | Fynd | Fil:rad | Fix |
|---|---|---|---|
| C1 | `promoteVariant` (kron-klicket) förstör grafen garanterat: removeNode raderar inbound/outbound-edges → föregående steg får 0 utgångar → klick gör INGENTING (tyst stuck). Autosave persisterar; nästa publish skeppar död funnel | quiz-graph.ts:324-345; VariantControls.tsx:79-82 | Peka om edges till vinnaren + kopiera primärens utgående edges innan siblings tas bort |
| C2 | Publish har NOLL grafvalidering - skeppar steg utan utgångar, edges mot raderade noder, quiz utan nåbar exit | quiz-publish.ts:177-243; api/quiz/[id]/publish/route.ts:41-47 | Validera: nåbara steg har ≥1 utgång, alla edge-referenser finns, ≥1 exit nåbar, trafficPct=100; 422 med lista |

## D. Blogg: exploderar vid återaktivering (autopilot AV för alla 3 workspaces sedan ~22 maj, utan larm)

| # | Fynd | Fil:rad | Fix |
|---|---|---|---|
| D1 | `resumeOrphanedPublishes` saknar statusfilter → auto-publicerar pending_review/rejected/unpublished. [LIVE] 32 hydro13-rader matchar IDAG (31 medvetet nedplockade) - mass-republiceras vid första cron-körningen, förbi gate+människa | blog-autopilot.ts:1137-1147 | `.in("status",["draft","published"])` + exkludera terminala statusar |
| D2 | DataForSEO-seeds hårdkodade per SPRÅK inte workspace → [LIVE, redan hänt] sömn-/kuddartiklar live på Renew-bloggen (kollagen) med Hydro13-CTA + dubbelgenerering | blog-autopilot.ts:808-856 | Seeds per workspace i settings; ingen fallback |
| D3 | Gate-fail återställer inte planraden (revertToPlanStatus anropas aldrig i gate-grenen) → [LIVE] kollagen-mage-tarm fast i writing sedan 25/5; vid återaktivering: regen-loop som bränner $0.5/körning + dubblett-pages | blog-autopilot.ts:392-433 | Sätt deferred + koppla page_id så loopen bryts |

## E. LP/publicering: live SEO-skada + tyst innehållsförstörelse

| # | Fynd | Fil:rad | Fix |
|---|---|---|---|
| E1 | Sitemap+blogglistor+länk-targets workspace-blinda [LIVE]: halsobladet-sitemapen innehåller doginwork-valpartiklar, hydro13-kollagen (varav unpublished), A/B-rest, dubbletter; Renew-artiklar länkar till halsobladet (cross-brand). SAMMA ROT som D2-klassen | cloudflare-pages.ts:851-857; blog-deploy.ts:142-180; internal-links.ts:279-338 | workspace_id-join i alla tre + mappning CF-projekt→workspace; dedupa slugs |
| E2 | Slug-kollisioner: happysleep OCH hydro13 båda published på samma halsobladet-URL:er (sista deploy vinner, andras published_url ljuger) [LIVE]; dubbletter inom workspace x3/x2 [LIVE]; pages-POST race + swipe-POST helt utan dubblettkoll | pages/route.ts:68-80; api/swipe/route.ts:112-139 | Unikt index pages(workspace_id,slug); publish-koll på (språk,CF-projekt); swipe-dubblettkoll |
| E3 | Manuell publish ignorerar blog_publish_target → hydro13-blogg via knappen hamnar på halsobladet.com istället för get-renew.com [LIVE-historik finns: 4 translations med published_url på båda domänerna = duplicate content utan canonical] | api/publish/route.ts:64-70 | Routa seo_blog till Shopify när target=shopify; backfilla published_url + 301:a/ta ner halsobladet-kopiorna |
| E4 | Editorn förstör inline-scripts + tredjeparts-iframes PERMANENT tyst: strippas vid load, första autosave (3s) skriver förlusten till DB, nästa publish deployar funktionsdöd sida. Kodkommentaren "Originals are preserved" är falsk | BuilderContext.tsx:991-1001, 617-708 | Spara undan strippade noder med placeholder + återinjicera vid extract |

## F. Segment-demographics läcker in i bot-promptar (bryter gårdagens beslut)

| # | Fynd | Fil:rad | Fix |
|---|---|---|---|
| F1 | segment_swap-iteration injicerar seg.demographics (med rå quiz-stats [LIVE: "Quiz: 23.5%..."]) i copy-botens prompt | iterate/route.ts:78 → brainstorm.ts:1813,1819 | Stryk demographics ur iterationContext |
| F2 | Bildbrief-boten får `Demographics: ...` i prompten (risk: stats renderas som text i bild) | static-ad-prompt.ts:762; callers generate-static:39, brainstorm/approve:95 | Ta bort demographics-raden ur TARGET SEGMENT |
| F3 | suggest-iterations har samma läcka, idag "skyddad" av trasig query (P2-fyndet nedan) - aktiveras när queryn fixas | suggest-iterations/route.ts:93 | Ta bort demographics samtidigt som query-fixen |
| F4 | doginwork-segmentet "Tidigt i problemet" har "Quizets majoritet." i DESCRIPTION → går in i Genesis-promptar | product_segments (DB) | Städa meningen ur description |

---

# P1 - Fastnade states, tysta fel, självläkning

## Quiz/data
- **Q1** Analytics + CSV-export tyst trunkerade till 1000 rader [LIVE: 1000 av 10 398 answers] - dashboarden/exporten räknar på slumpvis delmängd; variant-beslut/KPI:er på fel siffror. (01-avatar.md OK - togs direkt ur DB.) `analytics/route.ts:91-133,296-318; sessions/export/route.ts:46-49` → paginera med .range()-loop eller RPC:er.
- **Q2** Failad startSession = HELA sessionens events tappas + exit-redirect utan qz_sid → köp felattribueras "Direkt LP". `runtime App.tsx:249-283` → retry m backoff + buffra events före session finns (fixar även Q3-cold-start-svar).
- **Q3** createVariant klonar inga edges → ny variant = återvändsgränd för X% av trafiken, ingen varning, ingen publish-gate. `quiz-graph.ts:108-126` → kopiera originalets utgående edges.

## Meta-förvaltning (tysta schemafel = döda funktioner)
- **M1** [LIVE] auto_paused_ads saknar created_at (heter paused_at) → morning-briefens dedupe-query errorar TYST → redan pausade bleeders återkommer som action cards. `morning-brief/route.ts:884-888; zero-spend-alert:172-179`.
- **M2** [LIVE] concept_lifecycle saknar created_at (heter entered_at) → killed-dedupe död. `morning-brief/route.ts:893-898`.
- **M3** ad-performance-sync kan aldrig faila (failCronRun importeras men används aldrig) → token-död = "completed: 0 ads". `ad-performance-sync/route.ts:12,328-331`.
- **M4** Sync skippar hydro13+doginwork (kräver egen system_user_token som de saknar [LIVE]) - även återaktiverad synkar den bara happysleep vars ads är AV. `ad-performance-sync/route.ts:76-88` → env-token-fallback (samma semantik som getToken()).
- **M5** cleanup-empty-adsets räknar PENDING_REVIEW/IN_PROCESS som döda → kan pausa nypushat adset under Metas granskning; kör money-write via setMetaConfig ej runWithMetaConfig. `cleanup-empty-adsets/route.ts:136-190`.
- **M6** Budget-cooldown vilar helt på snapshots från sync-cronen; sync död → +20%-rec varje körning; autopilot-execute saknar egen recency-check för budgethöjningar. `strategy-engine.ts:477-501` → dedupe mot autopilot_actions 3d.

## Blogg
- **BL1** Gate-allowlisten saknar konkurrentdomänerna som skribent-prompten själv delar ut → jämförelseartiklar failar per definition (självdestruerande gate) [LIVE: exakt det fällde kollagen-mage-tarm]. `soft-gate.ts:141-175` → bygg allowlist från samma källa som prompten.
- **BL2** Gate check 6 hårdkodar halsobladet/get-renew → 100% av da/no-artiklar failar internal-links-checken vid återaktivering. `soft-gate.ts:126-138,192-207`.
- **BL3** Svenska hårdkodad för alla språk: user-prompt ("Write ENTIRE article in Swedish") + anti-slop injicerar svenska synonymer i da/no + bildprompter svenska. `blog-writer.ts:1230-1238,434-492; blog-images.ts:159,198-201`.
- **BL4** Reject-flödet skapar dubbletter (page-rader kvar, replan → ny page samma slug) [LIVE x3, x2]; rejected matchar dessutom orphan-resume (D1). `review/[id]/reject/route.ts:36-49`.
- **BL5** Approve stödjer bara Shopify (501 för CF) trots happysleep-gate PÅ; publicerar med placehold.co-bilder. `review/[id]/approve/route.ts:48-54`.
- **BL6** blog-images-retry kan fastna för evigt på nyaste failande artikeln (limit 1, ingen retry-räknare); skickar fel produktkontext till doginwork. `cron/blog-images-retry/route.ts:40-41,69-80`.

## LP/editor
- **L1** Bulk-bildöversättning = klientdriven kö: stängd flik → image_status='translating' för evigt; reconcile täcker INTE translations-tabellen; UI-texten "continues in background" är falsk. `TranslationRow.tsx:240-279; ImageSelectionModal.tsx:116-164` → server-side draining eller reconcile-utbyggnad.
- **L2** maxDuration 180 < Kie-poll 280s i translate-page-images + translate-image (gårdagens fix missade dessa två) → betalda renders dödas mitt i. → maxDuration 800. **Snabbaste fixen i hela auditen.**
- **L3** Swiper-import färdigställs i browsern (stängd flik = fast i importing trots betald Claude-körning); saveAndRefresh kollar inte res.ok ("Import complete!" vid fel). `ImportProgressPanel.tsx:385-411,439-492`.
- **L4** Batch-fel rapporteras bara om SISTA bilden felar (5 fel + sista ok = grönt). `translate-page-images/route.ts:272-294`.
- **L5** Stuck publishing/translating självläker bara när någon öppnar just den sidans detaljvy. → flytta till reconcile.

## Infra/observability
- **I1** 15 av 16 aktiva crons loggar INTE cron_runs [LIVE], gsc-sync/blog-images-retry/research-scan saknar dessutom Telegram → dör tyst. → cron-tracker-wrapper överallt + dead-man-watchdog.
- **I2** cron-status övervakar 3 crons borttagna sedan april [LIVE: stale + evig "running"-rad] → peka om + städa raden.
- **I3** Telegram utan fallback: nere = ALLA larm borta tyst. `telegram.ts:44-46` → Resend-fallback.

## Verktyg
- **V1** /review "Avvisa" på iterationer = no-op som rapporterar success (jobbet återuppstår i feeden för evigt); Approve köar aldrig för push. `approval-actions.ts:360-411` (latent, flödet oanvänt).
- **V2** Research deep-scan: 20-40 min arbete under 300s-cap (Hobby), nuggets upsertas EFTER loopen → kill = alla Haiku-anrop betalda, inget sparat, omkörning betalar igen. `research-scan.ts:33,267-352` → inkrementell upsert + dedupe på external_review_id.
- **V3** [LIVE] 12 video_jobs fast i generating sedan april → stale-sweep (lägg i reconcile).

---

# P2 - Datakorruption, sync, beräkningsfel

## Meta
- meta_page_config-upsert `onConflict:"country"` mot index (workspace_id,country) → POST 500:ar ALLTID, Settings-spar trasigt [LIVE]. `meta/page-config/route.ts:41-52`.
- meta_campaign_mappings unikt index GLOBALT (product,country,format) utan workspace → workspace B stjäl A:s rad. `campaign-mappings/route.ts:46-61` + DDL.
- meta_ad_performance saknar konto-kolumn → morning-brief/Ad Tracker blandar konton/valutor i ROAS/shares; apply_budget_shifts flyttar budget på blandad bas. `morning-brief/route.ts:82-87,619-662`.
- Bleeder-detektion hoppar över värsta fallen: 0 köp → cpaThreshold 0 → continue; [LIVE] pipeline_settings saknar doginwork-rad → deras bleeder-skydd helt av. `morning-brief/route.ts:394-397`.
- scale-cooldown missar Telegram-skrivna graduated_winner (utan workspace_id) → dubbla +20%. increase/reduce_budget saknar cooldown+tak. `actions/route.ts:152-161,402-506`.
- Campaign Builder: getAdSetConfig utanför runWithMetaConfig. `push/route.ts:105`. daily-snapshot: ROAS = total revenue / partiell spend (env-kontot). `daily-snapshot/route.ts:68-73`.

## Quiz
- Shopify-webhook-idempotens = tur + Metas 48h-dedupe (skyddet quiz_sessions.purchase_order_id täcker inte LP-köp/failade updates) → använd meta_capi_events.event_id som nyckel FÖRE sändning. `shopify-webhook/route.ts:323-400`.
- **KOLLA EVENTS MANAGER**: webhooken skickar CAPI Purchase för ALLA ordrar; om Shopify-kanalens egen pixel också skickar Purchase med annat event_id → dubbla köpsignaler → Meta optimerar mot fel CPA. (Manuell verifiering.)
- Rate limit 60/min per IP: mobil FB/IG-trafik = CGNAT → spike = 429, beacon-flush tappas permanent. `events/route.ts:20-44` → nyckla på session_id.
- Editorns autosave: lost-update-race utan versionskontroll (timmars variantjobb kan backas tyst). `QuizContext.tsx:44-64`.
- reset-analytics hard-raderar hela forskningsbasen utan spärr för publicerade quiz. `reset-analytics/route.ts`.

## Blogg
- [LIVE] DB/live-desync: 4 hydro13-translations published_url→halsobladet medan samma slugs live på get-renew (duplicate content, en svarar 200 på BÅDA); 31 "unpublished" = status appen inte känner. → backfill + 301 + standardisera statusar.
- Trustpilot Product-schema appliceras på ALLT (templateForCheck hårdkodad) → butiksrating som produktrating = review-snippet-spam-risk (latent tills trustpilot_domain sätts). `blog-autopilot.ts:972-975`.
- Affiliate-regexen matchar i HTML-attribut → <a> injiceras INUTI alt="..." = korrupt HTML; affiliate-domäner saknas i gate-allowlist (först riktiga länken failar gaten). `inject-links.ts:111-127`.
- Fabricerade test-claims uppmuntras i prompt ("We tested 7 products over 9 weeks...") + Erik Lindberg-personan med påhittade meriter - marknadsföringslagen + Googles reviews-policy på YMYL. `blog-writer.ts:1188-1203; blog-shell.ts:89-157`.
- Gate-fail loggar ingen LLM-kostnad (insert efter publish-steget). `blog-autopilot.ts:507-516`.
- Telegram-notiser: escTg för MarkdownV2 men parse_mode Markdown → synliga backslashes; review-pingen kan parse-faila tyst = gate-parkerad artikel utan ping. `telegram.ts:303`.

## LP
- Lost update i translate-page-images: HTML läses vid start, skrivs efter 1-3 min render → autosave-edits under fönstret försvinner. `translate-page-images:43-47 vs 138-156`.
- srcset orörd vid server-side bildbyte → publicerad sida visar originalspråksbilden. `translate-page-images:199-269`.
- translations.slug helt ovaliderad (blir CF-path + sitemap-URL; [LIVE] trasig slug live med strippade å/ä/ö). Duplicate-variant får samma slug → skriver över control. `translations/[id]/route.ts:91-98; duplicate/route.ts:36-47`.
- Deploy-manifest-race: två samtidiga publiceringar → sista deployen saknar den andras path → 404 tills nästa deploy (autopilot-cron + manuell publish = realistisk kollision). `cloudflare-pages.ts:362-396`.
- Storage-läckor: page-images/{id}/ städas aldrig; .list default-limit 100 = partiell städning; pages-DELETE städar inget; upload-image validerar inte translationId. Även translation-DELETE-cleanup körs aldrig vid pages-DELETE (cascade). `translations/[id]:306-318; pages/[id]:100-118`.
- ab_tests föräldralös: publishABTest finns inte längre (CLAUDE.md:71 ljuger), aktiv rad från feb pekar på återanvänd variant, /snarkning-skal live. → arkivera + uppdatera CLAUDE.md.
- CF-dedupe litar på DB-manifestet utan check-missing mot CF (samma klass som manifest-wipe-incidenten). `cloudflare-pages.ts:369-370`.
- SEO-metainjektion via String.replace: $-sekvenser tolkas, " bryter og-attribut, saknad <title> = tyst no-op. `translations/[id]:164-181`.

## Verktyg
- Kie-kostnader loggas som $0 i B/A + image-swiper + video-swiper [LIVE ≈ $16+ osynligt, video dyrast]; B/A loggar INTE alls vid timeout (betald bild utan spår, resultat bara i NDJSON-ström). `assets/before-after:872-895 m.fl.` → KIE_IMAGE_COST + logga direkt efter createImageTask.
- Slug-vs-UUID: iterate/suggest-iterations jämför product-SLUG mot product_id (uuid) → query failar ALLTID tyst [LIVE 22P02] → iterations-copy genereras utan guidelines/segments. `iterate:159-160; suggest-iterations:71-77` (fixa ihop med F3!).
- import-url: SSRF utan private-IP-guard, ext-fallback kringgår content-type, hela svaret i minne före size-check, lagras i publik bucket. `assets/import-url:51-94`.
- Sub-resurs-mutationer (segments/guidelines/references PATCH/DELETE) binder inte till produkten → cross-workspace-mutation möjlig. `products/[id]/segments/[segmentId]:39-78 m.fl.` → `.eq("product_id", productId)`.
- Video Swiper utan hård cap på scener (Gemini styr; varje scen = nano-banana + Veo/Kling). → slice(0,4).
- image-swiper products-lookup utan workspace-filter + 3 döda queries. `image-swiper:73-100`.

## Infra
- usage_logs-luckor: brand-check (Serper+Claude), brand-ideas, research-evaluate, expenses/process (5 anrop), headlines/suggest, hooks/generate-variation [LIVE: inga sådana typer i 30d-fördelningen].
- Storage-orphans vid page-delete (se LP ovan).

---

# P3 - Kvalitet/UX/städ (urval)

- **Quiz**: okända event_types filtreras tyst (så tappades cta_click); sendBeacon application/json kräver preflight vid unload (byt text/plain); steg-räknaren räknar variant-siblings ("27" när användaren ser 25); imageDescription renderas som synlig text när bild saknas; ImagePicker accepterar blob:/localhost-URL:er; events[] utan längdtak; webhookens capi-error-catch är död kod (supabase kastar inte); plaintext-email i event-meta.
- **Meta**: Campaign Builder dubbelpush-race kvar (restrisk LÅG - dubbletter skapas PAUSED); auto_paused_ads.adset_id null → zero-spend-korrelation död; oviktade avg(ctr/cpc); cron-status utan ålders-flagga.
- **Blogg**: usage_logs utan workspace_id; "2026" hårdkodat i titel-regel; anti-slop kör på hela HTML-strängen inkl. href; maxDuration 800 på Hobby (ljuger, cap 300).
- **LP**: usage_logs-inserts felkollas aldrig + swipe-dubbellogg; quiz-publish + review/approve otrimmade env-värden (exakt "smarthelse\n"-klassen!); import-poll utan transient-tolerans (Retry = hela Claude-rewriten igen = dubbel kostnad); indexbaserat bildbyte kan träffa fel bild; fetch-url läcker Chromium-process vid timeout; optimizeImages tar bara 30 första bilderna; workspace-scoping saknas i 6 service-role-routes; sitemap-lastmod ljuger (bumpas av osparade edits).
- **Verktyg**: research nuggets search-param → PostgREST-filterinjektion; deliverability ignorerar query-fel (tom men "frisk" UI); research_sources PATCH tar ...updates ovaliderat (inkl. workspace_id); /usage days=0 = hela tabellen utan limit; activity-feed days=abc → 500; brand-check cache-nycklar onormaliserade; invoice-check Promise.race avbryter inte processen (parallella IMAP-körningar); 107 unmatched + 10 error-invoices utan åtgärdsflöde [LIVE]; Amazon-källa aldrig skannad utan UI-signal [LIVE].
- **Infra**: middleware startsWith utan trailing slash genomgående; fetch utan timeout i kie/telegram/openrouter/gethookd/dataforseo/loop/klaviyo.

---

# Rekommenderad fixordning

1. **Säkerhets-svepet** (kirurgiskt, ~halvdag): B1-B4 (auth/middleware/webhook-secret) + F1-F4 (demographics-läckan, ~10 rader) + L2 (maxDuration, 2 rader).
2. **Meta-avväpningen** (oavsett A1-beslutet): A2 staleness-spärr + A3 runWithMetaConfig + A4 Telegram-avväpning + A5 guard-ordning + M1/M2-schemafixar. Sen WILLIAMS BESLUT på A1 (återaktivera sync/guards för Renew+doginwork kräver även M3/M4).
3. **Quiz-grafintegritet + data**: C1+C2+Q3 (graf), Q1 (1000-radersfixen), Q2 (session-retry). Manuell koll: Events Manager dubbla Purchase-källor.
4. **Blogg-pre-flight** (MÅSTE före återaktivering): D1+D3+BL1+BL2 minst; D2+BL3 för multi-språk.
5. **LP-saneringen**: E1+E2+E3 kod + prod-datastädning (sitemap-förorening, halsobladet-dubbletter, 31 unpublished, ab_tests-arkivering, CLAUDE.md-uppdatering). Sen E4 (editor-strippningen).
6. **Självläknings-utbyggnad**: reconcile-cronen täcker translations.image_status/pages.importing/swipe_jobs/video_jobs (L1/L3/L5/V3) + cron_runs-wrapper på alla 16 crons + dead-man-watchdog + Telegram-fallback (I1-I3).
7. **Kostnadssynlighet**: $0-Kie-fixarna + usage_logs-luckorna + V2 (research-scan-checkpointing).
8. **P2-resten + P3** i mån av tid.

# Öppna beslut för William

1. **A1**: Återaktivera ad-performance-sync + zero-spend-alert (+ ev. bleeder-guards) för Renew/doginwork - eller dokumentera 100% manuell förvaltning? (Rekommendation: återaktivera sync+alert; guards kan vänta tills metrics flödar igen.)
2. **Events Manager-kollen** (quiz P2): dubbla Purchase-källor för doginwork-pixeln?
3. **Blogg-återaktivering**: vill du ha hydro13-autopiloten igång igen efter pre-flight-fixarna, eller ska den förbli av?
4. **ab_tests + /snarkning-skalen**: arkivera/radera?
5. **De 2 döda "Valpakademin Sales Page"-stubbarna** (från igår, fortfarande obesvarat): rensa?

Rådata: agent-transkript i sessionen 2026-07-07. Prod-verifieringar read-only via PostgREST + publika curl.
