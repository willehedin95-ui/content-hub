# Session: 2026-08-06 (heldag) - Studion gjordes om i sex varv + HEALTH-kontot med autopilot

Allt arbete i `izabella-v2/`. Inga content-hub-kodändringar. Slutcommit `328621d`, deploy-cache `izabella-v2-94`, live `izabella-v2.pages.dev/studio`.

**Startdokument för nästa session: `izabella-v2/docs/HEALTH-HANDOVER.md`** (nya kontot) och `izabella-v2/docs/STUDIO-HANDOVER.md` (all teknik). Detaljerad varv-för-varv-logg: `2026-08-06-studio-build-button.md`.

## What was done (komprimerat)

**Studion (Izabellas inredningskonto), sex UX-varv på Williams löpande feedback:**
1. Sexstegsflödet → EN "Build a post"-knapp (swipe→dekonstruktion→koncept→bild→reel→plan).
2. Bildförst-feed (reels ihopfällda), bildscrape av alla konton (78 nya poster, selin 72x-carousel).
3. JSON-extraktionsprompting (hubbens image-swiper-recept porterat) - "väldigt lik, aldrig identisk".
4. Referensbild provades → "för likt" → ut igen; regeln SAMMA FORMAT, ANNAT HEM (objekt som klass).
5. Konceptsida per bygge (original + lightbox + takes + val + dag), Make = DB-driven verkstad som överlever reload, Today-fliken borttagen (To post överst i Plan), 1 take default + "One more take".
6. Buggar fixade i farten: iz_concepts.assets `[]`-arrayfällan (assets försvann tyst), carousel byggde från inre slides i stället för cover, refresh-race dubblade kort, db() kastade på return=minimal (tidigare session).

**HEALTH-kontot (Williams eget, nytt):**
- brand-kolumn på competitors/concepts/queue; växlare i studion; BRANDS.health + BRAND_SYSTEMS-hälsorubrik; ingest/vet/score `--brand`.
- Discovery: 13 seeds → 115 poster → vet (8 avvisade) → score (@nutritiontactics 100%/10.0 = bästa källan).
- `tools/autopilot.py`: mät täckning → välj källa (percentil×fit, bara vettade) → bygg via studions API:er (service-role-access i requireUser) → auto-godkänn taken → reel → schemalägg. Cron-säker, dry-run default.
- Två skarpa autonoma varv körda. Varv 1 textlös (Williams dom: "säger inte ett skit") → text-i-bild blev BRAND-REGEL (interior=ren bild, health=infografiken ÄR innehållet) → varv 2 levererade nattscen med claim i stor typ, schemalagd, noll klick.

## Decisions made

- Health: bygg utan konto först, engelska/globalt, bred vinkel (skärps på data). Williams svar via frågerunda.
- 1 take default (3 nästan identiska = illusion för 54 credits), "One more take" 18 cr på konceptsidan.
- Ingen referensbild till nano-banana (för likt); JSON-extraktionen ensam bär formatet.
- Text-i-bild är per brand, aldrig delad regel.
- Autopiloten vägrar ovettade konton by design.
- Publish-steget väntar på IG-konto + Meta-token (Williams bord); reels är tysta stills så API-publicering har noll ljud-nackdel för DETTA konto.

## Current state

Fungerar, verifierat mot prod: hela Build-kedjan båda brands, konceptsidor, autopilotens två varv, 5,3-6,1s-reels i bucketen. Izabellas två chrome era-koncept ligger på Make och väntar på hennes/Williams val. Healths varv-2-post schemalagd 2026-08-06 (postas inte - inget konto).

## Blockers / Open questions

- 🔴 IG-konto + FB-sida + Meta-token för health (bara William).
- 🔴 Izabella-spåret: delningsarket på riktig iPhone fortfarande otestat; nischmening + första postens datum.
- Graph API standard access-frågan (30 min utredning, gäller båda kontona).
- Claim-tonen i health-varv 2 på gränsen ("cannot do both") - bevaka.

## Next up

1. William: skapa IG-konto för health → jag bygger publish-steget + cron.
2. Insights-läsning via Graph API (stänger mätloopen för BÅDA kontona).
3. Izabella: iPhone-testet + datum → börja posta (verktyget är inte flaskhalsen).
4. Autopilot --horizon 7 när William vill fylla en vecka.
