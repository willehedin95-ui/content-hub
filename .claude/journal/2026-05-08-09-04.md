# Session: 2026-05-08 09:04 CEST

## What was done

- **Diagnosed**: doginwork quiz analytics showed 0 sessions because the deployed quiz HTML had `apiBaseUrl: "https://content-hub-three.vercel.app"` baked in. That alias belongs to QuasyTech Inc (a stranger's Vercel project). Every `/api/quiz/session` and `/api/quiz/events` POST silently 404'd. `quiz_sessions` table had 0 rows ever.
- **Fixed Vercel `APP_URL`** for Production, Preview, Development → `https://content-hub-nine-theta.vercel.app` (the actual content-hub deploy alias).
- **Republished doginwork quiz** via `npx dotenv -e .env.local -- tsx scripts/publish-doginwork-quiz.ts` so live HTML now has the correct apiBaseUrl.
- **Hardened `src/lib/quiz-publish.ts`** with `verifyApiBaseUrl()` - sends OPTIONS to `/api/quiz/session` with allowed origin and requires `Access-Control-Allow-Methods: POST, OPTIONS` in response. Throws before publish if URL points to wrong deployment. Commit `732573b`, pushed to main, build + 227 tests passed pre-push.
- **Verified end-to-end**: 2 real Meta-driven sessions landed in `quiz_sessions` within 9 minutes after republish (Galaxy S22 Ultra + A52s, Facebook in-app browser, utm_source=facebook, campaign 120223696566130176).
- **Documented** in `doginwork/SESSION-LOG.md` (top entry) and `~/.claude/projects/-Users-williamhedin-Claude-Code/memory/quiz-funnel-builder.md` (4 new gotchas in Hot gotchas section).

## Decisions made

- **Republish via local script with `.env.local`** rather than via prod publish endpoint. Reason: William's local `.env.local` already had the correct `APP_URL`, so the script generated the right HTML immediately without waiting for a Vercel redeploy of the env var change.
- **OPTIONS-based probe over POST/HEAD**: OPTIONS is idempotent, doesn't create DB rows, and our handler always returns 204 with distinctive `Access-Control-Allow-Methods: POST, OPTIONS` - which a stranger's 404 page won't have.
- **Did NOT touch `BLOCKED_TRACKING_IPS`** to bypass William's IP for self-testing. Instead waited for real Meta traffic. Reason: avoiding state changes on prod env that I'd then have to remember to revert.

## Current state

- ✅ doginwork quiz tracking is live and writing sessions.
- ✅ Pre-publish probe (`verifyApiBaseUrl`) deployed on main as commit `732573b`.
- ✅ William's IP `94.254.62.220` still on `BLOCKED_TRACKING_IPS` (intentional - filters self-tests from analytics).
- ⚠️ The 2 verified sessions both have `answers: {}` - users landed on the quiz but didn't progress past landing. Could be normal early bounce, or first-question UX issue. Worth checking analytics once more traffic accumulates.

## Blockers / Open questions

- None for this fix.
- Side note: Pre-existing uncommitted changes on `main` from prior sessions (`src/lib/quiz-publish-shell.ts`, `src/types/quiz.ts`, `.claude/journal/LATEST.md`) - look like work-in-progress Clarity provider integration. Left untouched.

## Next up

1. Watch quiz analytics tomorrow for real session/answer/completion data once Meta traffic accumulates.
2. If Meta sessions consistently show `answers: {}` (i.e., users don't progress), inspect first-question/landing UX (potential JS error, slow render, or layout issue on Facebook in-app browser specifically).
3. If `BLOCKED_TRACKING_IPS` ever needs cleanup and we lose track of what's in it, regenerate from `.env.local` (currently `94.254.62.220`).
