# Session: 2026-05-03 18:28 - Doginwork quiz polish + image pipeline + FP framework

Massive day on the Doginwork Valpakademin quiz. Started with offer-page IA fixes, ended with image optimization pipeline, Higgsfield MCP setup, and an A/B test grounded in Funnel Professor framework.

## What was done

### Quiz architecture changes
- **CopyBlocks framework analysis** mapped against current offer page; concluded our offer is correctly conditions-heavy (per quiz-funnel docs the conviction work happens upstream in the quiz, not on offer page)
- **Built parallel test offer-page v21** (`offer_html_body_v21.py`) per consult's structural rewrite - 12 sections vs 19, hero = personalized diagnosis, mechanism before Marie, matched testimonial via JS conditional. Accessible via `?goto=v21`
- **Removed dead-data questions**: b4 (improve_goals), b7 (rumsrenhet redundant with b9), b13 (personality), b14 (socialisering slider), b21 (self-assessment slider), b10 (samspel-importance slider), b20 (leka). Quiz went from 31 → 24 steps
- **Reformulated b5** from "Ignorerar {name} dig?" (loaded word, breaks Marie's mental model) to "Hur ofta tittar {name} upp på dig under en promenad?" - same data, neutral framing
- **Added b16ba yes/no** "Har du undrat om {name} är 'svårare' än andra valpar?" with conditional routing to b16c (YES path: validation-opening with 3 Christine quotes + 70% stat) or b16c_no (NO path: "Skönt att höra" + same naming body). Both converge at b19
- **Added b16c puppy-blues validation slide** - Liven-style naming-drop ("Det heter valpdepression"), Dogo "Regret Getting a Puppy"-pattern (brand says taboo thoughts FOR user), 70% stat from ManyPets 2023 survey with source disclaimer
- **Time validation b22b** got Christine quote prepended ("Jag hinner inte ens ducha")
- **Reordered**: b17 social proof moved after b19/b20 to break interstitial-back-to-back pattern
- **Fixed b17 headline** removing "Valpakademin" name-drop per CLAUDE.md hard rule

### Mobile UX
- **Keyboard-aware CTA**: VisualViewport listener in App.tsx sets `--quiz-keyboard-inset` CSS var. Both `.quiz-continue-wrap` and `.quiz-question-bottom` now use `bottom: var(--quiz-keyboard-inset, 0)` with smooth transition. Fixed-bottom CTA pushes above iOS/Android keyboard on text-input + dropdown steps

### Landing image
- Generated new landing-puppy.png via Higgsfield Nano Banana 2 (golden retriever lying on cream blanket, no toys/hands - matched brief perfectly)
- Replaced old `hero-leka.png` (French bulldog with tennis ball - "after-state" mismatch) on landing slide

### Image optimization pipeline
- New `scripts/optimize-quiz-assets.ts` - sharp-based webp converter, q82 effort 6
- Idempotent: skip-logic uses `updated_at` timestamps (Supabase x-metadata-* headers don't persist)
- Hooked into `publish-doginwork-quiz.ts` - runs automatically before every publish
- Bulk-converted 42 files: **6.63 MB saved** (landing-puppy 2.1MB → 63KB at -97%, profile-hero 1.2MB → 40KB at -97%, valp-maskot 989KB → 92KB at -91%)
- Updated all `.png`/`.jpg` references → `.webp` in build-quiz.py + offer_html_body.py + offer_html_body_v21.py

### Higgsfield MCP setup
- Installed via `claude mcp add --transport http higgsfield https://mcp.higgsfield.ai/mcp` (subdomain, not `higgsfield.ai/mcp` which is just marketing page)
- OAuth flow + token refresh working
- Saved setup notes to memory: `higgsfield.md` topic file with model preferences (nano_banana_flash default, soul_2 cheap UGC, nano_banana_2 strict prompt-following), credit costs, prompt patterns, gotchas (Unlimited toggle in web UI doesn't propagate to MCP API)
- Updated MEMORY.md pointer

### Funnel Professor framework
- Read FP newsletter "Building, testing, iterating quiz funnels"
- Synthesized to `quiz-knowledge/09-funnel-professor-pillars.md` - 3-pillar framework (positioning + graduality + personalization), KPI targets (Q1 50-70% / completion 20-30% / completion-to-purchase 10%+), Q1-first iteration playbook, application checklist, doginwork-specific findings
- Registered in `index.ts` + `FULL_KNOWLEDGE` + README

### A/B test for landing slide
- Created `landing_a` (control) + `landing_b` (test) sharing variantGroupId, both 50% trafficPct
- Variant A: "Få din valps personliga träningsplan" (current, generic plan-builder framing)
- Variant B: "Hitta din valps största beteendeproblem" (FP "specific revelation" framing per WW3 example)
- Both have outgoing edge → b1 Kön
- Live: `?goto=A+control` and `?goto=B+specific`

## Decisions made

- **Skipped fritext input** despite my initial enthusiasm - violates docs' "use sparingly, forces cognitive load" rule. William called it correctly
- **Used statement-format over questions for emotional content** - puppy-blues quotes shown as social proof ("many parents say this") instead of forcing user to admit ("do you feel this?"). Validation without extraction
- **Kept the b16ba yes/no even though answer doesn't change underlying education** - useful for format-rotation + sets up either YES validation or NO acknowledgment opening
- **Skipped credit-charged "unlimited" web UI toggle** - confirmed via MCP that subscriber unlimited mode doesn't propagate to API. Used Nano Banana 2 (1.5 credits/gen) as default per William's preference
- **Did NOT push commit to main** - per content-hub CLAUDE.md hard rule, only push on explicit user confirmation

## Current state

### Live
- Quiz at `quiz.doginwork.se/valpakademin` published 2026-05-03T15:46:40 UTC
- 24 steps in main flow + landing A/B + offer v21 parallel + b16c_no NO-path = 28 total nodes
- All 42 images optimized to webp on Supabase Storage (originals retained)
- A/B test running on landing slide (variantGroupId, 50/50 split)
- Build passing (verified `npm run build`)
- Committed locally to content-hub: `69f7495 quiz: keyboard-aware CTA + image webp pipeline + FP pillars knowledge`

### Working
- Image optimizer idempotent on re-runs (verified: 0 conversions, 42 skipped)
- VisualViewport keyboard handling
- Conditional routing (yes/no path on b16ba)
- All goto-shortcuts: `?goto=Promenad-fokus`, `?goto=Difficulty`, `?goto=v21`, `?goto=Puppy+blues`, `?goto=A+control`, `?goto=B+specific`

### Broken / unmeasured
- **Quiz analytics broken** - CORS error on `/api/quiz/session` blocks per-step start/completion tracking. Audit v58 noted as console error but it's actually a measurement blocker
- **Without analytics, A/B test is flying blind** - can't measure variant Q1 start rates

## Blockers / Open questions

1. **Analytics CORS fix needed** before A/B test gives any value. Endpoint URL: `content-hub-nine-theta.vercel.app/api/quiz/session`. Origin `quiz.doginwork.se` blocked
2. **Ad-to-quiz congruency check pending** - Maries Meta ads pitch "kursen Valpakademin", quiz promises "personlig träningsplan". Plan ≠ kurs. Per FP this is positioning #1. Need to either pivot ads or pivot quiz
3. **William hasn't tested the new flow on his mobile yet** - changes are extensive (24-step flow, conditional routing, new emotional architecture)

## Next up

Priority order:

1. **Fix analytics CORS** so A/B test produces measurable data. Without this, we're optimizing on instincts only - violates FP's "profit > metrics, but you still need metrics"
2. **Audit Maries active Meta ads** for ad-to-quiz congruency. Either retune ads (lighter lift) or retune quiz (bigger lift)
3. **William mobile-walks the full quiz** end-to-end to validate emotional flow + A/B variants. Look for friction we missed
4. **Set A/B test deadline** - run for 1-2 weeks or ~500 sessions per variant before evaluating
5. **Consider future tests after Q1 normalizes** per FP iteration playbook - next-worst drop-off question becomes the target
