# Session: 2026-03-25 ~08:00

## What was done
- **Author byline with avatar**: Replaced old `.blog-shell-meta` div with circular avatar photo + "Av Erik Lindberg" + date, injected after hero image in article HTML
- **Mobile padding fix**: Reduced shell container to 12px, article to 8px 0, hero-img margin to 4px to eliminate double-padding issue
- **Mobile table fix** (3 iterations):
  1. First tried card layout (stacked rows with data-label pseudo-elements) — user said "looks like shit"
  2. Then tried hiding columns 3+ — user wanted scoring visible
  3. Final: hide middle columns, show first + last (Kudde + Betyg) via `:not(:first-child):not(:last-child){display:none}`
- **WebP image optimization**: Blog images were serving as raw PNG from Supabase (~1.8MB each). Added `optimizeImages()` call to both `publishBlogArticle()` and republish script. Now converts to WebP via sharp (80% quality), deployed to CF Pages edge. Hero images: 1808KB → 48KB (97% smaller).
- **Blog writer table instruction**: Added rule #12 telling Claude to put the 2 most important columns first since mobile only shows first + last.
- **Republished all 4 articles** with all fixes applied

## Decisions made
- **First+last column** over card layout for mobile tables — much cleaner, less vertical space, keeps it looking like a table
- **Image optimization at publish time** (not at generation time) — keeps original PNGs in Supabase for editing flexibility, serves optimized WebP from CF edge
- **Generic CSS column hiding** (`not(:first-child):not(:last-child)`) rather than article-specific rules — works for any column count

## Current state
- All 4 published blog articles have: author avatar, WebP images, mobile-friendly tables
- Blog autopilot pipeline now auto-optimizes images on publish (no code change needed for future articles)
- Commit `855ffcc` on main, NOT pushed (auto-deploys on push)

## Blockers / Open questions
- Some articles still have placeholder images (`placehold.co`) that need real images generated (those before the blog-images.ts feature)
- The `blog_featured_image_url` in DB still points to Supabase PNG (used for og:image). This is fine for now since social crawlers need absolute URLs.

## Next up
1. Push to deploy (user decides when)
2. Pause ads pointing to dead domains (coolafynd.se, woofie.se) — burning ad spend
3. Test blog autopilot end-to-end with image generation on Vercel
4. Tune translation quality review prompt
