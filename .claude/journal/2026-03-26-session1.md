# Session: 2026-03-26 morning (session 1)

## What was done
- **FB Group source naming fix**: Imported FB group comments were appearing under "Manual Research" with the Hub URL as source name. Fixed DB record (renamed to "FB Groups"), split `facebook_group` into its own section in ResearchSources.tsx.
- **Real platform logos**: Installed `react-icons`, replaced placeholder lucide icons with actual brand icons (Trustpilot, Reddit, Amazon, Facebook, Instagram, TikTok) on both Sources and Feed. Gotcha: `SiAmazon` doesn't exist in `react-icons/si` — use `FaAmazon` from `react-icons/fa`.
- **Fixed star ratings**: Empty stars now have `fill-gray-200` (was just border color).
- **Removed Flashback.org**: Cleaned from apify.ts, research-scan cron, sources route, feed, and sources UI.
- **Research Feed readability redesign**: Constrained cards to `max-w-3xl`, moved platform logo to LEFT of source name, separated star ratings onto own row, removed Tag icons from tag buttons, made tags more subtle, tightened spacing. Much less cognitive overload.
- **Manual scan buttons**: Added per-source "Scan Now" button (spinning RefreshCw icon) to all auto-scraped sources. New shared `scanSingleSource()` in `src/lib/research-scan.ts`, new API endpoint `POST /api/research/sources/[id]/scan`. Shows result feedback ("12 scraped, 5 nuggets stored") for 8 seconds.
- **PageSpeed monitoring** (from another session): New Speed tab in SEO section with PageSpeed Insights API integration.

## Commits
- `d64c233` — Add dedicated Facebook Groups section in Research Sources
- `bc19e01` — Add real brand logos to Research sources and nuggets, fix filled stars
- `2144d5b` — Remove Flashback.org from research sources
- `c3f6c36` — Redesign research feed for readability + add manual scan buttons
- `73d27fc` — Add PageSpeed monitoring tab to SEO section

## Key files
- `src/components/research/ResearchFeed.tsx` — Redesigned nugget cards, PlatformIcon component
- `src/components/research/ResearchSources.tsx` — FB Groups section, brand logos, scan buttons
- `src/lib/research-scan.ts` — Shared scan function (extracted from cron)
- `src/app/api/research/sources/[id]/scan/route.ts` — Manual scan API endpoint

## What's next
- Test manual scan buttons on live sources
- Consider refactoring the daily cron to also use `scanSingleSource()` from the shared lib (currently duplicated logic)
- User may want further feed refinements after testing the new layout
