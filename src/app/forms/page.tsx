import FormsInboxClient from "@/components/forms/FormsInboxClient";

export const metadata = { title: "Formulär - Content Hub" };

export default function FormsPage() {
  return <FormsInboxClient />;
}
